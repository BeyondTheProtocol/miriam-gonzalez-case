[build]
  command = "npm_config_build_from_source=true pnpm rebuild better-sqlite3 && pnpm generate && pnpm update-fundraiser"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10"
